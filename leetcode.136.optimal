class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        xor = 0

        for i in nums:
            xor = xor ^ i

        return xor


        # Optimal, Solution  :: Use bitwise XOR to remove duplicates  a^a=0
                                                                      a^0=a    #
